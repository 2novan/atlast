<div class="filters">
  <strong>
    <p><i class="fas fa-ticket-alt"></i> Upcoming concerts</p>
  </strong>
</div>
<div class="cards-releases">
  <% @concerts.order(start_at: :asc).each do |concert| %>
      <%= render "shared/concert_card", concert: concert %>
  <% end %>
</div>
