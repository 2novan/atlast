
const slideSubmit =() => {

//   const pagination_bullet = document.querySelector(".swiper-pagination-bullet");
//   const pagination_bullet_active = pagination_bullet.classList.contains("active");
//   const pagination_3 = pagination_bullet.innerHTML;


//   button.addEventListener("slide changed", event => {
//     if (pagination_bullet_active && (pagination_bullet.innerHTML == "3") ) {
//       console.log("It's working");
//     }
//   });
}

export { slideSubmit };
